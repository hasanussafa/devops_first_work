pipeline {
    agent {
        label 'python3'
    }
    stages {
        stage('Run Python Script for add two numbers') {
            steps {
                sh 'python3 add2numbers.py'
            }
        }
    }
}
